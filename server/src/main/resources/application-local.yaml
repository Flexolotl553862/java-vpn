spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/java_vpn_server
    username: test
    password: test
    driver-class-name: org.postgresql.Driver

  liquibase:
    change-log: classpath:db/changelog/changelog-master.yaml

tun:
  mock: echo
  auto:
    enabled: true
    address: 10.2.1.1

logging:
  level:
    com.stevesad: DEBUG
    org.pcap4j: WARN